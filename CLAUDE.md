# test

This repository currently has no application code — it's a starting point. This file holds repo-level instructions for Claude Code; update it as the project takes shape.

---

## Coding Standards (Non-Negotiable)

Before writing, generating, or editing any code in this repo — HTML, CSS, TypeScript/JavaScript, React components, or code shown in chat — **invoke the `fes-coding-standards` skill first** and follow it. This applies automatically to every code change, not just when explicitly requested. Do this every session, not just the first time this file is read.
